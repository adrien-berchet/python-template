[![CI]({{ download_url }}/actions/workflows/ci.yml/badge.svg)]({{ download_url }}/actions/workflows/ci.yml)
[![Coverage](https://codecov.io/gh/{{ repository_namespace }}/{{ repository_name }}/branch/main/graph/badge.svg)](https://codecov.io/gh/{{ repository_namespace }}/{{ repository_name }})
{% if pypi %}[![PyPI](https://img.shields.io/pypi/v/{{ distribution_name }}.svg)](https://pypi.org/project/{{ distribution_name }})
{% endif %}[![License](https://img.shields.io/github/license/{{ repository_namespace }}/{{ repository_name }})]({{ download_url }}/blob/main/LICENSE.txt)

# {{ project_name }}

{{ project_description }}

This is where you should write a short paragraph that explains what the package
does, how it is intended to be used, and why it exists.

Source | <{{ download_url }}>
:--- | :---
Issues | <{{ tracker_url }}>
{% if sphinx %}Documentation | <{{ docs_url }}>
{% endif %}
{% if setup_container %}Container | <https://ghcr.io/{{ repository_namespace }}/{{ repository_name }}>
{% endif %}

## Installation

```bash
python3 -m pip install {{ distribution_name }}
```

To install from the repository instead:

```bash
python3 -m pip install git+{{ download_url }}.git
```

For local development, a `uv`-based workflow is recommended:

```bash
uv venv
source .venv/bin/activate
uv pip install --group dev --all-extras -e .
```

## Examples

Library usage:

```python
from {{ package_name }} import __version__
from {{ package_name }}.example import add

print(__version__)
print(add(1, 2))
```

CLI usage:

```bash
{{ distribution_name }} --version
{{ distribution_name }} -x 1 -y 2
```{% if sphinx %}

<!-- README only content. Anything below this line won't be included in index.md -->

See {{ docs_url }} for more detailed documentation.{% endif %}
