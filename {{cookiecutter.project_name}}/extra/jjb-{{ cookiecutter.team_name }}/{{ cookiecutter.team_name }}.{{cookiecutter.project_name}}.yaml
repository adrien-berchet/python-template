- project:
    name: '{{ cookiecutter.project_name }}'
    git_project: '{{ cookiecutter.gerrit_repo }}'
    jobs:
      - '{{ cookiecutter.team_name }}.python.{name}.gerrit'
      - '{{ cookiecutter.team_name }}.python.{name}'
